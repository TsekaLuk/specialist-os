# Product Hardening

Implement the remediation order approved after the product audit. Preserve real
evidence and frozen Core scope. Do not bypass native capture restrictions.

## Acceptance

- CI and release collect pytest tests; frontend build and command tests run in CI.
- Copied POSIX commands round-trip JSON, whitespace and quotes without execution.
- Recorded-result pages expose no ineffective editable run settings.
- Resampling honors requested channels, verified against real FFmpeg output.
- Demo/slides reconstruction and clean installation have documented, verified paths.
- Unified workspace and native playback/recording retain explicit open status
  until actually verified; do not archive this entire task after partial fixes.
